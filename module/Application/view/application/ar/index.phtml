<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 * @var list<Application\Entity\Ar> $items
 */
?>

<section class="crud-page">
    <div class="crud-page__topbar">
        <a href="<?= $this->url('home') ?>" class="crud-back-link">
            &larr; Voltar
        </a>
    </div>

    <header class="crud-hero">
        <div class="crud-hero__content">
            <span class="crud-hero__eyebrow">Cadastro vinculado</span>
            <h1 class="crud-hero__title">AR</h1>
            <p class="crud-hero__text">
                Gerencie autoridades de registro e acompanhe seus vínculos com AC N2 e AC.
            </p>
        </div>

        <div class="crud-hero__actions">
            <a href="<?= $this->url('ars/new') ?>" class="crud-btn crud-btn--primary">
                Novo AR
            </a>
        </div>
    </header>

    <?php if (empty($items)): ?>
        <section class="crud-empty">
            <div class="crud-empty__icon">+</div>
            <h2 class="crud-empty__title">Nenhum AR cadastrado</h2>
            <p class="crud-empty__text">
                Crie um novo registro para começar a relacionar autoridades de registro ao sistema.
            </p>
            <a href="<?= $this->url('ars/new') ?>" class="crud-btn crud-btn--primary">
                Criar primeiro AR
            </a>
        </section>
    <?php else: ?>
        <section class="crud-table-card">
            <div class="crud-table-card__header">
                <div>
                    <h2 class="crud-section-title">Lista de ARs</h2>
                    <p class="crud-section-subtitle">
                        Total de registros: <strong><?= count($items) ?></strong>
                    </p>
                </div>
            </div>

            <div class="crud-table-wrap">
                <table class="crud-table crud-table--wide">
                    <thead>
                        <tr>
                            <th class="crud-table__th crud-table__th--id">ID</th>
                            <th class="crud-table__th">Nome</th>
                            <th class="crud-table__th">AC N2</th>
                            <th class="crud-table__th">AC</th>
                            <th class="crud-table__th crud-table__th--actions">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                    <?php foreach ($items as $item): ?>
                        <?php
                            $id = (int) $item->getId();
                            $acn2 = $item->getAcN2();
                            $ac = $acn2->getAc();
                        ?>
                        <tr class="crud-table__row">
                            <td class="crud-table__td crud-table__td--id">
                                <span class="crud-badge crud-badge--neutral">#<?= $id ?></span>
                            </td>

                            <td class="crud-table__td">
                                <div class="crud-entity__title">
                                    <?= $this->escapeHtml($item->getName()) ?>
                                </div>
                            </td>

                            <td class="crud-table__td">
                                <div class="crud-entity__title">
                                    #<?= (int) $acn2->getId() ?> — <?= $this->escapeHtml($acn2->getName()) ?>
                                </div>
                            </td>

                            <td class="crud-table__td">
                                <div class="crud-entity__title">
                                    #<?= (int) $ac->getId() ?> — <?= $this->escapeHtml($ac->getName()) ?>
                                </div>
                            </td>

                            <td class="crud-table__td crud-table__td--actions">
                                <div class="crud-actions">
                                    <a
                                        href="<?= $this->url('ars/view', ['id' => $id]) ?>"
                                        class="crud-btn crud-btn--ghost crud-btn--sm"
                                    >
                                        Ver
                                    </a>

                                    <a
                                        href="<?= $this->url('ars/edit', ['id' => $id]) ?>"
                                        class="crud-btn crud-btn--ghost crud-btn--sm"
                                    >
                                        Editar
                                    </a>

                                    <button
                                        type="button"
                                        class="crud-btn crud-btn--info crud-btn--sm"
                                        onclick="openQrModal(
                                            '<?= $this->escapeHtmlAttr($this->url('ars/qrcode', ['id' => $id])) ?>',
                                            'Conteúdo do QR: /ars/<?= $id ?>'
                                        )"
                                    >
                                        QRCode
                                    </button>

                                    <form
                                        action="<?= $this->url('ars/delete', ['id' => $id]) ?>"
                                        method="post"
                                        class="crud-inline-form"
                                        data-delete-form
                                        data-delete-name="<?= $this->escapeHtmlAttr($item->getName()) ?>"
                                    >
                                        <input
                                            type="hidden"
                                            name="csrf"
                                            value="<?= $this->escapeHtmlAttr($this->csrfToken('ar_delete_' . $id)) ?>"
                                        >

                                        <button
                                            type="button"
                                            class="crud-btn crud-btn--danger crud-btn--sm"
                                            onclick="openDeleteModal(this)"
                                        >
                                            Excluir
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </section>
    <?php endif; ?>
</section>