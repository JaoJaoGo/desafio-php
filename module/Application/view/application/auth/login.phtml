<?php
$error = !empty($this->error) ? $this->error : null;
?>

<div class="login">
    <div class="login__header">
        <span class="login__eyebrow">Área restrita</span>
        <h1 class="login__title">Entrar</h1>
        <p class="login__subtitle">
            Acesse sua conta para continuar no sistema.
        </p>
    </div>

    <?php if ($error): ?>
        <div class="alert alert--error" role="alert">
            <?= $this->escapeHtml($error) ?>
        </div>
    <?php endif; ?>

    <form class="form" method="post" action="<?= $this->url('login') ?>">
        <input
            type="hidden"
            name="csrf"
            value="<?= $this->escapeHtmlAttr($this->csrfToken('login_form')) ?>"
        >

        <div class="form__group">
            <label class="form__label" for="email">Email</label>
            <input
                class="form__input"
                id="email"
                type="email"
                name="email"
                required
                autocomplete="email"
                placeholder="seuemail@empresa.com"
            >
        </div>

        <div class="form__group">
            <label class="form__label" for="password">Senha</label>
            <input
                class="form__input"
                id="password"
                type="password"
                name="password"
                required
                autocomplete="current-password"
                placeholder="Digite sua senha"
            >
        </div>

        <div class="form__actions">
            <button class="btn btn--primary btn--block" type="submit">
                Entrar no sistema
            </button>
        </div>
    </form>

    <p class="login__footer">
        Desafio técnico em PHP com foco em organização, clareza visual e experiência de uso.
    </p>
</div>